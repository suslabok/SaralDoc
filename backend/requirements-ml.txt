# Optional heavier NLP tier — adds spaCy English NER and multilingual
# transformer NER (for Nepali entities). Needs a machine with >1GB RAM
# and internet access on first run (models download from Hugging Face /
# GitHub release assets and are cached locally after that).
#
# Install AFTER requirements.txt:
#   pip install -r requirements.txt -r requirements-ml.txt
#   python -m spacy download en_core_web_sm
#
# Not recommended for free-tier hosting — torch alone is ~500MB.

-r requirements.txt
spacy==3.8.15
transformers==5.15.0
torch==2.13.0
